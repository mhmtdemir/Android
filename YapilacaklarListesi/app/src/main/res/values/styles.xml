<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/DarkBlue</item>
        <item name="colorPrimaryDark">@color/SlateBlue</item>
        <item name="android:windowSoftInputMode">stateHidden</item>
        <item name="android:dialogTheme">@style/dialog_light</item>
    </style>

    <style name="spinner_style" >
        <item name="android:background">@drawable/spinnerstyle</item>
        <item name="android:layout_marginLeft">5dp</item>
        <item name="android:layout_marginRight">5dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingBottom">5dp</item>
    </style>

    <style name="dialog_light" parent="@android:style/Theme.Dialog">
        <item name="@android:windowBackground">@color/SeaGreen</item>
        <item name="@android:textColor">@color/white</item>
    </style>

    <style name="CheckBoxTheme" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/SeaGreen</item>
        <item name="colorControlActivated">#9C27B0</item>
    </style>

    <style name="PopupMenu">
        <item name="android:panelBackground">@color/SeaGreen</item>
        <item name="android:textColor">@color/White</item>
    </style>
</resources>
